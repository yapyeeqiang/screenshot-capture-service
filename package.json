{
  "name": "layer-chrome-aws-lambda",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "sst test",
    "start": "sst start",
    "build": "sst build",
    "deploy": "sst deploy",
    "remove": "sst remove"
  },
  "eslintConfig": {
    "extends": [
      "serverless-stack"
    ]
  },
  "dependencies": {
    "@serverless-stack/cli": "0.68.1",
    "@serverless-stack/resources": "0.68.1",
    "aws-cdk-lib": "2.15.0",
    "chrome-aws-lambda": "^10.1.0",
    "puppeteer": "^13.5.1",
    "puppeteer-core": "^13.5.1"
  }
}
